name: Build Unified ChatGPT Marketing Skill

on:
  push:
    branches: [main]
    paths:
      - 'package-src/**'
      - 'scripts/**'
      - '.github/workflows/build-bundle.yml'
      - 'README.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build and validate ChatGPT skill
        run: python scripts/build_bundle.py

      - name: Show generated package
        run: |
          unzip -l dist/chatgpt-marketing-unified.zip | sed -n '1,180p'

      - name: Upload ChatGPT skill ZIP
        uses: actions/upload-artifact@v4
        with:
          name: chatgpt-marketing-unified
          path: dist/chatgpt-marketing-unified.zip
          if-no-files-found: error
          retention-days: 90
